import { RouteDrawer } from '../../../../components/common/modals/route-focus-modal/route-drawer';
import { CreateMakeForm } from '../components/forms/create-make-form';

const ModelCreatePage = () => {
  return (
    <RouteDrawer>
      <CreateMakeForm />
    </RouteDrawer>
  );
};

export default ModelCreatePage;
