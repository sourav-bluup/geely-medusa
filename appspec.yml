version: 0.0
os: linux
files:
  - source: /
    destination: /var/devops
permissions:
  - object: /var/devops
    owner: root
    group: root
hooks:
  ApplicationStart:
  - location: scripts/UpdateImageTag.sh
    runas: root
    timeout: 120
